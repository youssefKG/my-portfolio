const initial = {
  x: 500,
  scale: 0,
  opacity: 0,
};
const animate = {
  x: 0,
  scale: 1,
  opacity: 1,
};
const transition = {
  duration: 0.7,
};
const viewPort = {
  once: true,
};

export { initial, animate, viewPort, transition };
